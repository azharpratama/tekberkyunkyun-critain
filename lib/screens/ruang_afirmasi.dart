import 'package:flutter/material.dart';

class RuangAfirmasiPage extends StatelessWidget {
  const RuangAfirmasiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Ruang Afirmasi")),
      body: const Center(child: Text("Halaman Ruang Afirmasi")),
    );
  }
}
