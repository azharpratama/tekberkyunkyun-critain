import 'package:flutter/material.dart';

class RuangBerceritaPage extends StatelessWidget {
  const RuangBerceritaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Ruang Bercerita")),
      body: const Center(child: Text("Halaman Ruang Bercerita")),
    );
  }
}
